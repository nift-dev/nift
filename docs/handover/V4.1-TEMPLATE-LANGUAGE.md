# Nift v4.1 template-language campaign

Status: active

This file is the authoritative handover for the v4.1 template-language campaign. Update it at every checkpoint. The normal Nift model remains deliberately small (`@input`, `@content`, `@path`); these features are optional advanced templating.

## Frozen language contract

- `$[x := expr]`: declare a mutable binding in the current scope. `:=` is right-associative and may shadow an outer binding.
- `$[x = expr]`: assign the nearest visible existing mutable binding. `=` is right-associative and never declares.
- Top-level declaration/assignment interpolation emits nothing, although assignment/declaration expressions evaluate to the assigned value internally for chaining.
- Types are inferred at declaration and stable for the lifetime of that binding; incompatible reassignment is an error.
- `$[const x := expr]`: shallow-const binding (no rebinding).
- `$[immut x := expr]`: deep read-only binding/view; no global freeze of mutable aliases.
- Child scopes inherit visible bindings and may mutate existing outer mutable bindings. New declarations are local. `@if`, `@for`, `@input`, `inject()`, `@fn` calls and `@fragment` calls use this model.
- No `global` escape in v4.1.
- `@:=(name){ expression }`: generic multiline declaration; outer braces are directive framing, not part of the expression.
- `inject(path)`: expression-source injection. Parse/evaluate the file as Nift source at the expression position; no extension-based typing. It creates a child scope and a tracked dependency.
- `validate(schema, value)`: validate and return `value` unchanged on success.
- `@input` remains normal template/output inclusion.
- Loop variables are local. Iteration is live over the underlying collection; rebinding the source variable does not retarget an active iterator.
- `@fn(name(args)){...}` returns an ordinary value; `@fragment(name(args)){...}` returns rendered template content.
- Both are invoked only as `$[name(args)]`, preserving unknown `@...` passthrough such as CSS `@media`.
- Old `@json` forms are retained until declaration/inject/validate parity is proven, then reconciled/deprecated.
- Queues, stacks, maps, priority queues, structs and classes are out of scope for v4.1.

## Acceptance rules

Every checkpoint is a separate commit. Existing-template behavior, dependency tracking and passthrough are regression gates. Run relevant focused tests plus the full suite at architectural boundaries. Measure performance during development; do not accept material regressions in ordinary templates that do not use the new language.

## Checkpoints

CP0 contract/handover; CP1 v4.1.0 development; CP2 Jsonic++ v1.0.0; CP3 Minify++ v1.1.3; CP4 lexical scope foundation; CP5 `:=`; CP6 `=`; CP7 stable inferred types; CP8 scope certification; CP9 `const`; CP10 `immut`; CP11 multiline `@:=`; CP12 `inject`; CP13 `validate`; CP14 `@json` reconciliation; CP15 live-loop certification; CP16 callable contract; CP17 `@fn`; CP18 `@fragment`; CP19 cross-feature wall; CP20 parser fuzzing; CP21 dependency/incremental certification; CP22 performance; CP23 docs/migration; CP24 v4.1 certification.

## Checkpoint ledger

### CP0 — campaign contract and handover
- Commit: this checkpoint commit (`Create v4.1 template-language campaign handover`).
- Semantics changed: none; contract frozen for implementation.
- Tests added: none.
- Gates run: documentation/diff hygiene.
- Performance result: N/A.
- Rejected alternatives: implicit global declarations; `@name(...)` user invocation; extension-driven injection typing; keeping type-specific declaration syntax as the future model.
- Next checkpoint: CP1 — begin v4.1.0 development.

### CP1 — begin v4.1.0 development
- Commit: this checkpoint commit (`Begin Nift v4.1.0 development`).
- Semantics changed: none; development identity moved from released v4.0.13 to v4.1.0.
- Tests added: version-consistency expectations advanced to 4.1.0.
- Gates run: version consistency and diff hygiene.
- Performance result: N/A.
- Rejected alternatives: continuing on 4.0.x despite the planned language-surface changes.
- Next checkpoint: CP2 — import released Jsonic++ v1.0.0.

### CP2 — import released Jsonic++ v1.0.0
- Commit: this checkpoint commit (`Import Jsonic++ v1.0.0`).
- Semantics changed: none in the template language; embedded JSON implementation advanced to the certified v1.0.0 release state (`88e4736`).
- Tests added: none; upstream Jsonic++ tests imported with the release.
- Gates run: embedded Jsonic++ test wall plus Nift JSON/schema focused tests.
- Performance result: imports the release state established as conformant RapidJSON-class performance; no Nift-specific performance claim added.
- Rejected alternatives: importing Jsonic++ v1.0.1 development.
- Next checkpoint: CP3 — import released Minify++ v1.1.3.

### CP3 — import released Minify++ v1.1.3
- Commit: this checkpoint commit (`Import Minify++ v1.1.3`).
- Semantics changed: none in Nift templates; embedded minifier advanced to released v1.1.3.
- Tests added: upstream Minify++ release tests imported.
- Gates run: Minify++ smoke/semantic focused wall plus Nift build integration.
- Performance result: imports the performance-certified v1.1.3 release, not v1.1.4 development.
- Rejected alternatives: importing the current Minify++ development head.
- Next checkpoint: CP4 — lexical scope foundation.

### CP4 — lexical scope foundation
- Commit: this checkpoint commit (`Add v4.1 lexical binding scope foundation`).
- Semantics changed: none yet; parser now owns explicit nested variable scope frames alongside existing control-flow scope boundaries.
- Tests added: existing scope/control-flow suite remains the compatibility oracle.
- Gates run: build plus existing expression/control-flow focused tests.
- Performance result: scope frames are empty unless v4.1 bindings are used; ordinary lookup path unchanged.
- Rejected alternatives: extending the flat `json_bindings_` map with ad-hoc mutation flags; copying inherited values into each child scope.
- Next checkpoint: CP5 — add `:=` declaration.

### CP5 — `:=` declaration
- Commit: this checkpoint commit (`Add inferred template variable declarations`).
- Semantics changed: `$[x := expr]` declares in the current scope, supports right-associative chaining and shadowing, and emits nothing at top-level interpolation while retaining an internal value.
- Tests added: declaration, chaining, duplicate-local declaration, structured literal and non-rendering cases.
- Gates run: parser compile plus focused template-variable smoke.
- Performance result: variable lookup is only attempted for expression identifiers; existing directive parsing remains unchanged.
- Rejected alternatives: implicit declaration through `=`; mandatory explicit type annotations; rendering declaration values.
- Next checkpoint: CP6 — add `=` assignment.

### CP6 — `=` assignment
- Commit: this checkpoint commit (`Add template variable assignment`).
- Semantics changed: `=` assigns the nearest visible existing mutable binding, is right-associative, never declares, and top-level assignment emits nothing.
- Tests added: outer mutation, chained assignment, undefined-name rejection and comparison/operator disambiguation.
- Gates run: parser compile and focused expression tests.
- Performance result: assignment search walks scope depth only when an assignment operator is present.
- Rejected alternatives: implicit declaration on unknown assignment; copied child environments.
- Next checkpoint: CP7 — enforce stable inferred types.

### CP7 — stable inferred binding types
- Commit: this checkpoint commit (`Enforce stable inferred template binding types`).
- Semantics changed: declaration fixes a binding's inferred value category; incompatible reassignment errors. Numeric literals distinguish integer-valued and double-valued bindings.
- Tests added: compatible/incompatible scalar and structured reassignment cases.
- Gates run: parser compile and focused assignment/type cases.
- Performance result: assignment adds one constant-time type-category check after RHS evaluation.
- Rejected alternatives: dynamically changing binding types; mandatory declaration annotations.
- Next checkpoint: CP8 — certify lexical block semantics.

### CP8 — lexical block-scope certification
- Commit: this checkpoint commit (`Apply lexical scopes to template inputs`).
- Semantics changed: `@if`, `@for` and now `@input` execute in child variable scopes; inherited outer bindings remain mutable, while declarations made inside the construct die on exit.
- Tests added: shadowing, outer mutation, local lifetime and input-scope cases.
- Gates run: parser compile and existing input/control-flow compatibility checks.
- Performance result: one empty scope frame push/pop per nested construct; no copying of inherited bindings.
- Rejected alternatives: copy-on-entry environments; input-local inability to mutate caller bindings.
- Next checkpoint: CP9 — add shallow `const` bindings.

### CP9 — shallow `const` bindings
- Commit: this checkpoint commit (`Add shallow const template bindings`).
- Semantics changed: `$[const x := expr]` declares a current-scope binding that cannot be rebound; declaration remains non-rendering and type-stable. Container deep immutability is intentionally deferred to `immut`.
- Tests added: const declaration/read, reassignment rejection, shadowing and outer-const protection.
- Gates run: full Nift build; template-variable smoke; control-flow smoke; JSON smoke; JSON Schema integration; v4.1 version consistency. Jsonic++ v1.0.0 `StrNumber` schema compatibility and explicit duplicate-key rejection coverage were reconciled during integration.
- Performance result: mutability is one binding metadata flag checked only on assignment.
- Rejected alternatives: `Const`/`CONST` deep-const spellings; making `const` recursively immutable.
- Next checkpoint: CP10 — add `immut` deep-readonly bindings.
