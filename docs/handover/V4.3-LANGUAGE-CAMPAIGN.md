# Nift v4.3 language campaign handover

## Scope

This is the agreed game plan for the next Nift language campaign. It is not release authorization. Database/API access is deferred. Preserve the v4 principle: useful build/template facilities are native Nift features; do not restore LuaJIT.

Target surface:

```text
++ / --
+= -= *= /= %=
64-bit numeric review
mutable arrays
stack
queue
prique
map / sorted_map
set / sorted_set
lambdas
first-class callable values
lexical closures
higher-order functions
map/filter/reduce/any/all/find/count/sort transformations
```

`map` and `set` preserve insertion order. Consider `fast_map` / `fast_set` only later if profiling demonstrates a real performance need. `prique` is the public spelling for priority queue.

## Collection contract

Arrays gain:

```nift
push(value)    pop()
insert(i,v)    remove(i)
first()        last()
indexOf(value) contains(value)
size()         empty()
clear()
```

Frozen for v4.3: `first()`, `last()` and `pop()` fail on an empty array; `indexOf()` returns `-1` when absent; `insert()` accepts indices `0..size`; `remove()` requires an existing index. `string += string` is supported. `sort()` mutates the collection and returns the collection result; custom comparators use a boolean "a before b" contract. `stack` uses `top()`. `prique ==` uses identity semantics. `same()` rejects scalar values. Initial map keys/set members are stable scalar values only (bool/int/double/string; null is excluded).

`stack` is LIFO; `queue` is FIFO. Keep their APIs conventional and small (push/pop, top/front or a frozen `peek` spelling, size/empty/clear, contains where useful). Their `for` traversal order must be documented.

`prique` is priority ordered. Do not expose heap representation or iterators. It does not need structural equality. Preferred contract: `==` uses identity semantics, like structs/callables, and `same(a,b)` explicitly tests identity. CP1 may instead choose to reject `prique ==`; do not define equality by heap representation or logical dequeue sequence.

`map` preserves insertion order for iteration. Updating an existing key should retain its position; removing then reinserting should place it at the end. Map equality compares key/value contents and ignores insertion history.

`sorted_map` iterates by key comparison but has the same logical key/value equality semantics.

`set` preserves insertion order. Set equality compares membership and ignores insertion history. `sorted_set` iterates by value comparison while equality remains membership-based.

## Equality, identity, ordering and hashing

Do not add JS-style coercive equality or `===`.

```text
null        value equality
bool        value equality
string      value equality
numbers     numeric equality, including int/float cross-comparison
array       structural equality
stack       structural sequence equality
queue       structural sequence equality
map         structural key/value equality; insertion order ignored
sorted_map  structural key/value equality
set         structural membership equality; insertion order ignored
sorted_set  structural membership equality
struct      identity equality
callable    identity equality
prique      identity equality preferred; non-comparable is acceptable if frozen at CP1
```

Examples:

```nift
1 == 1.0              // true
1 == "1"             // false
[1,2] == [1,2]       // true
```

Add explicit identity testing:

```nift
a := [1,2,3]
b := a
c := copy(a)

a == c       // true
same(a,b)    // true
same(a,c)    // false
```

Freeze `same()` on scalar/non-reference values at CP1; prefer a clear type error rather than making it an alias for `==`.

Mutable compound values must not accidentally become unsafe hash keys. Initially prefer stable scalar map keys (bool/int/float/string, optionally null). If `1 == 1.0`, hash lookup must treat them equivalently. Set member eligibility must be frozen explicitly: either restrict to safe hashable values initially or use a representation that safely supports broader structural values.

Before sorted collections, sort or default `prique` comparison, freeze total-ordering rules. At minimum numbers (including int/float cross-comparison) and strings are orderable. Do not invent arbitrary ordering for arrays, structs, collections or callables.

## No cyclic user data graphs

Do not permit cyclic user-visible data/object graphs. Mutations that would create direct or indirect cycles through mutable compound values must fail deterministically. Add adversarial tests. Structural equality/copy/deepcopy need not support arbitrary cyclic user data.

Closures necessarily retain lexical environments internally; manage those lifetimes safely without exposing arbitrary cyclic data graphs. Recursive functions remain valid control flow.

## Numeric review

Preferred integer contract: signed 64-bit (`std::int64_t` or equivalent exact-width semantics).

Floating contract: plain C++ `double` (IEEE-754 binary64 on supported targets). Do not use `long double`; the extra precision is not worth its platform variability/cost for Nift. Verify Linux/macOS/Windows behavior.

Review int/int, float/float and int/float equality/order; promotion; compound assignment; overflow/underflow; ++/-- boundaries; modulo; formatting/interpolation; JSON interaction; and real Nift performance.

## Operators

Add:

```nift
i++  i--  ++i  --i
x += y
x -= y
x *= y
x /= y
x %= y
```

These are real lvalue operations. Prefix returns the updated value; postfix returns the old value; targets are evaluated exactly once. Test locals, mutable accessible struct fields and indexed/keyed lvalues where existing assignment semantics support them. Freeze string `+=` explicitly.

## First-class callables and lambdas

Named functions and lambdas share one callable value model. They can be assigned, passed, returned, stored where ordinary values are legal, and invoked indirectly.

Lambda syntax:

```nift
x => x * 2
(a, b) => a + b
() => 42

x => {
    if(x < 0) { return -x }
    return x
}
```

A user-defined lambda is simply a lambda expression assigned to a value:

```nift
double := x => x * 2
add := (a,b) => a + b
result := double(10)
```

Avoid a redundant second `lambda(...)` declaration syntax unless parser evidence requires it.

## Closures

Closures capture lexical **bindings**, not value snapshots:

```nift
factor := 10
multiply := x => x * factor
factor = 20
multiply(2)             // 40
```

Mutable captured state and escaping closures are intended:

```nift
fn(counter(start)) {
    count := start
    return () => count++
}

next := counter(10)
next()                  // 10
next()                  // 11
```

Captured environments must safely outlive declaring frames. Freeze copy/deepcopy, `same`, callable identity, private-member access, recursion and error interactions.

## Higher-order transformations

Both named functions and lambdas work wherever a callable is accepted. Agreed family:

```text
map filter reduce any all find count sort
```

Examples:

```nift
doubled := values.map(x => x * 2)
evens := values.filter(x => x % 2 == 0)
total := values.reduce((acc,x) => acc + x, 0)
has_large := values.any(x => x > 100)
all_valid := values.all(x => x >= 0)
first_large := values.find(x => x > 100)
n := values.count(x => x > 100)
values.sort((a,b) => a < b)
```

Freeze no-match `find`, comparator semantics, sort mutation-vs-copy behavior, map callback argument shape, and result type/order. Do not force every transformation onto every collection when semantics are unnatural.

## CP2 architecture map

The current expression runtime is centered in `Parser::evaluate_expression`; bindings are `VariableBinding` objects owning shared `json::Document` storage, structs use explicit instance identities, callable frames use parser-managed scopes, and array/object literals use Jsonic documents. v4.3 extends these shared seams rather than adding a second evaluator. Array aliases intentionally share binding storage; `copy`/`deepcopy` produce distinct array storage. Existing assignable struct-member paths remain the lvalue boundary until indexed/keyed assignment itself is introduced.

Numeric storage keeps `double` for floating values. Nift integer expressions use signed-64-bit semantics; integer spellings outside binary64's exact range retain exact decimal spelling internally and integer arithmetic checks signed-64-bit overflow rather than silently rounding.

## Architecture rule

Build shared runtime concepts for ownership/identity, structural equality, `same`, comparison, hash/key eligibility, copy/deepcopy, cycle prevention, lvalues, internal iteration, callable invocation and captured lexical bindings. Do not build each collection as an unrelated special case. Iterators remain internal; users get `for` and collection operations, not iterator objects.

# Step-by-step implementation checklist

Reconcile exact CP numbering with repository history, but preserve dependency order. Each CP needs focused tests and evidence before becoming the next baseline.

## Phase 0 — freeze the contract

- [x] **CP0 Baseline:** record Nift/regression-suite/website HEADs and worktrees; run the current full local and external contract.
- [x] **CP1 Contract tests only:** freeze `prique ==` (identity preferred), scalar `same`, set member/hash rules, array empty/not-found behavior, stack/queue peek spelling, comparator rules, sort mutation behavior, map reinsertion and string `+=`.
- [x] **CP2 Architecture map:** trace parser, lvalues, Value representation, function frames, copy/deepcopy, struct identity, JSON/interpolation and `for` traversal before production edits.

## Phase 1 — numeric/value foundations

- [x] **CP3:** migrate/certify signed 64-bit integers and boundary behavior.
- [x] **CP4:** certify `double` floating representation and cross-platform semantics; explicitly reject `long double`.
- [x] **CP5:** certify int/float equality, comparison, promotion and compatible hashing. (Mixed int64/double comparison was incomplete; closed by the pre-certification review correction in `Parser.cpp` — see review-correction record below.)
- [x] **CP6:** centralize structural vs identity equality and add `same()`.
- [x] **CP7:** centralize orderability and hash/key eligibility.
- [x] **CP8:** reject direct/indirect cyclic user data mutations; certify equality/copy/deepcopy termination.

## Phase 2 — operator ergonomics

- [x] **CP9:** `+= -= *= /= %=` with exactly-once lvalue evaluation.
- [x] **CP10:** prefix `++x --x`.
- [x] **CP11:** postfix `x++ x--`.
- [x] **CP12:** precedence, nested-expression, field/index/key, interpolation and error regression gate.

## Phase 3 — mutable arrays

- [x] **CP13:** `push/pop/insert/remove/clear` plus alias/copy/deepcopy semantics.
- [x] **CP14:** `first/last/indexOf/contains/size/empty`, using structural equality for searches.
- [x] **CP15:** invalid/empty cases, nested values, large-array and performance/memory gate.

## Phase 4 — sequential collections

- [x] **CP16:** shared internal collection protocol (iteration/mutation/size/copy/equality hooks).
- [x] **CP17:** stack with LIFO semantics and structural sequence equality.
- [x] **CP18:** queue with FIFO semantics and structural sequence equality.
- [x] **CP19:** `prique` default scalar ordering, identity and copy behavior without heap leakage. Custom callable priority comparators are deferred until the callable/transformation layer is complete.
- [x] **CP20:** sequential-collection interaction, cycle, determinism and performance gate. (Cycle rejection was missing; closed by the pre-certification review correction — see below.)

## Phase 5 — maps and sets

- [x] **CP21:** insertion-ordered `map`, key rules, reinsertion, structural mapping equality.
- [x] **CP22:** `sorted_map`, key ordering and type/comparator errors.
- [x] **CP23:** insertion-ordered `set`, member rules and membership equality.
- [x] **CP24:** `sorted_set`, value ordering with membership equality.
- [x] **CP25:** associative interaction gate: int/float equivalence, copy/deepcopy, cycles, large workloads, deterministic order. (Cycle rejection was missing; closed by the pre-certification review correction — see below.)
- [x] **CP26:** profile insertion-order overhead. Record evidence; do not add `fast_map`/`fast_set` unless justified.

## Phase 6 — first-class callables/lambdas

- [x] **CP27:** shared callable runtime representation and identity.
- [x] **CP28:** named functions as assignable/passable/returnable/storable values and indirect calls.
- [x] **CP29:** expression lambdas: `x => expr`, `(a,b) => expr`, `() => expr`.
- [x] **CP30:** block lambdas with normal statement/return behavior and source-aware errors.
- [x] **CP31:** callable parity/identity/storage/indirect-call regression gate.

## Phase 7 — lexical closures

- [x] **CP32:** capture lexical bindings, not snapshots; reads observe later mutation.
- [x] **CP33:** writes through closures update shared captured bindings.
- [x] **CP34:** escaping closure environments safely outlive declaring frames.
- [x] **CP35:** nested/returned closures, multiple closures sharing state, identity and private-access interactions.
- [x] **CP36:** ownership/leak/sanitizer/adversarial lifetime and forbidden-user-cycle gate.

## Phase 8 — higher-order transformations

- [x] **CP37:** `map`.
- [x] **CP38:** `filter`.
- [x] **CP39:** `reduce`, including empty/initial-accumulator contract.
- [x] **CP40:** `any` and `all`, including short-circuit behavior.
- [x] **CP41:** `find` and `count`, including no-match behavior.
- [x] **CP42:** `sort`, default/comparator ordering, stability decision and mutation-vs-copy contract.
- [x] **CP43:** transformations across applicable arrays/maps/sets/other collections; callback argument shapes and order preservation.
- [x] **CP44:** higher-order/callback failure propagation, nested transformations and performance gate.

## Phase 9 — certification and documentation

- [x] **CP45:** full local regression wall plus standalone `nift-regression-suite`; preserve independent black-box coverage.
- [x] **CP46:** sanitizers/Valgrind where available, malformed/adversarial inputs, lifetime and memory checks.
- [x] **CP47:** benchmark expression/operator/collection/callable microcases plus representative single/10k Nift builds; compare baseline.
- [ ] **CP48:** reconcile README, source docs, handovers, ReleaseNotes and pending website documentation; no public publish yet.
- [ ] **CP49:** update website source docs/examples/navigation for the accepted language surface and build locally; do not deploy without approval.
- [ ] **CP50:** final release-candidate audit: clean worktrees, provenance, full test/performance evidence, known limitations and explicit STOP before tag/push/release/publication.

## Explicitly deferred

Do not expand this campaign into build-time database/API access, libcurl, SQLite, LuaJIT, explicit iterators, package management, FFI, threads or arbitrary shell execution. Those can be investigated independently after this language campaign.

## Current implementation status — CP0–CP34

CP0–CP34 are implemented in the current workspace. CP15 adds the larger array sanity/performance gate. CP16–CP20 add the shared internal collection representation plus `stack`, `queue` and `prique`; `@for` consumes collections without exposing iterator objects. CP21–CP26 add insertion-ordered `map`/`set`, sorted variants, logical equality, copy/identity behavior and a profiling gate. No `fast_map`/`fast_set` variant is justified by current evidence.

CP27–CP31 add first-class named callable values, expression lambdas and block lambdas with identity and indirect invocation. CP32–CP34 establish lexical binding capture, mutable captured state and escaping closure lifetimes. Binding capture uses a shared binding slot so rebinding is visible to closures without changing ordinary array alias/rebinding semantics.

Focused evidence is in `tests/v43_cp15_cp34_smoke.sh`, alongside `tests/v43_cp0_cp14_smoke.sh`. The CP15–CP34 smoke includes collection semantics/order, logical equality, `same()`, first-class named calls, expression/block lambdas, binding-not-value capture, closure writes, escaping closures and a 1,000-element array gate. On this workspace the combined CP15–CP34 smoke completed in approximately 0.03 s with about 3.4 MiB peak RSS; treat that only as a local regression datum, not a cross-platform benchmark.

CP35 and CP37–CP44 are now implemented. CP36 and CP45–CP50 remain certification/documentation/release-candidate work; memory tooling, sanitizer/Valgrind depth, cross-platform performance evidence, website publication, and the final release-candidate audit are intentionally left for the follow-up campaign.


## CP35 / CP37–CP44 implementation update

CP35 certifies returned closures and multiple closures sharing one captured binding (including callable values stored through mutable arrays). CP37–CP44 add higher-order methods to arrays and applicable native collections. `map`/`filter` return arrays; `reduce` requires an explicit initial accumulator; `any`/`all` short-circuit; `find` returns `null` when no value matches; `count` returns the number of matches. Map callbacks receive `(key, value)`. Array `sort()` mutates in place, is stable, uses natural numeric/string ascending order by default, and accepts a boolean less-than comparator such as `(a,b) => a > b` for descending order. Inline lambdas inside method calls are parsed as nested expressions rather than being mistaken for a top-level lambda. Focused evidence is `tests/v43_cp35_cp44_smoke.sh`.

The development version is now `4.3.0` in the executable and Snap metadata. No tag, push, release, package publication, or website deployment is authorized by this workspace.

## Independent CP0-CP44 review (pre-CP36/CP45-CP50)

An independent implementation/tests/regression-suite/website review pass was
run before the remaining certification checkpoints. It did not begin CP36 or
CP45-CP50.

### Defects found and fixed (committed)

- `aaebf37` — numeric cast UB: `numeric_binary`'s int64 eligibility used
  `<= (double)INT64_MAX` (which rounds to 2^63), so a `9223372036854775808.0`
  double was cast to int64_t (UB) and produced a wrong result; the bound is now
  strictly `< 2^63`. Also restored the documented v4.2 fractional-modulo
  rejection (`1.5 % 1` must error), which the v4.3 double path had silently
  dropped.
- `aaebf37` — `deepcopy` returned the identical collection instance instead of
  cloning it (`same(s, deepcopy(s))` was true); collections now deep-clone.
- `aaebf37` — rendering a callable/collection value leaked the internal
  `\x1fnift:callable:/collection:` reference; the struct render guard is
  extended to those refs too.
- `aaebf37` — an expression lambda whose body failed to evaluate made the call
  site (`$[f()]`) render literally with no build error (the body error matched
  the "unknown value or malformed expression" passthrough prefix); lambda body
  errors now propagate as build failures.
- `0afccfa` — the three `v43_cp*_smoke.sh` files existed but were never wired
  into `make test`; added a `test-v43-language` target, included it, and added
  `tests/v43_review_smoke.sh` encoding the review contract (64-bit boundaries,
  operators, collection ordering/equality, deepcopy cloning, render-leak
  rejection, closures, higher-order semantics, lambda errors).

### Independent regression-suite expansion (committed)

- `1bad73a` — `contract/v43_language_smoke.sh` black-box v4.3 contract and the
  legacy version assertion advanced to v4.3.0. Full suite: 35/35 PASS.

### Website documentation corrections (committed)

- website stage `8d1d0ee` / public `06256f0` — corrected the `@for`
  map-example to the implemented object tuple form
  `@for((key, value) : mapping)` and removed source comments whose stripped
  alignment produced trailing whitespace in the generated output.

### Findings reported, not fixed (contract/design gaps)

- **Struct self-reference cycles are not rejected.** `@struct(node){next :=
  other()}` followed by `a.next = a` creates a cycle; the frozen contract says
  cyclic-mutation attempts must fail deterministically. It does not crash
  (structural equality on structs is identity; `deepcopy`'s seen-map handles
  it), but the invariant is not enforced.
- **Collection methods cannot be invoked through struct-field member paths**
  (`obj.c.size()` / `obj.c.set(...)` fail for a struct field holding a
  collection). Member-path method dispatch for collection/array methods is not
  implemented.
- **`@for` over a map requires string keys**; maps with numeric keys cannot be
  iterated, even though numeric keys are supported by the map API.
- **Mixed int64/double ordering beyond the exact int64 range errors**
  (`9223372036854775807 < 1e19` fails rather than returning true).
- **Array/collection methods require a binding root**; inline literals such as
  `[3,1,2].sort()` are not supported.
- **Array literals cannot contain struct/callable/collection references**
  (literal parsing is JSON); `push` of a reference value works.
- `contract/filesystem_recovery_smoke.sh` showed a timing flake in this
  environment (passed on re-run); not a v4.3 production change.

CP36 (closure ownership/leak/sanitizer certification) and CP45-CP50 remain not
started.

## Pre-certification contract closure (review-correction record)

Before CP36/CP45-CP50, the two frozen-contract violations found by the
independent review were resolved, plus one additional CONTRACT_VIOLATION.

- **Forbidden user-visible cycles now fail deterministically.** `Parser::reference_would_cycle`
  walks struct fields and collection values (struct/collection references are
  graph edges; callables are leaves because their captured lexical environments
  are internal; array values are plain copies) and rejects any mutation that
  would create a direct or indirect cycle: struct field assignment (member and
  receiver paths), collection `push`, and map `set` value. Self-references,
  indirect struct cycles, longer cycles, and collection->struct->collection
  cycles are rejected; acyclic sharing, aliases, copy/deepcopy and callables in
  collections remain legal. Covered by `tests/v43_review_smoke.sh` and
  `contract/v43_language_smoke.sh`.
- **Mixed int64/double comparison is complete across the full range.** A unified
  `numeric_compare` compares exact int64 operands without lossy conversion; a
  double is handled by magnitude so `9223372036854775807 < 1e19` is `true`,
  `9223372036854775807 < 9223372036854775808.0` is `true`, `INT64_MIN` and
  negative-large-double cases work, and equality/ordering always agree
  (e.g. `9007199254740992 == 9007199254740992.0` with `<`/`>` both false).
  CP5/CP20/CP25 are now genuinely satisfied and remain numbered as before.
- **Map `@for` iterates typed entries directly (no JSON-object
  materialization).** `@for((key, value) : map)` binds the original typed key
  and value from the map's ordered entry sequence, preserving the key domain
  and ordering (insertion for `map`, sorted for `sorted_map`) without
  stringification or a temporary object representation. `1` and `"1"` (and
  `true` and `"true"`) are distinct iterated keys, `m.get(key)` round-trips the
  typed key during iteration, and `1`/`1.0` remain one logical numeric key.
  This closes the CONTRACT_VIOLATION where maps with numeric keys could not be
  iterated and supersedes the earlier stringifying materialization.

Classification of the other reported limitations:

| Limitation | Classification | Action |
|---|---|---|
| Collection methods via struct-field member paths (`obj.c.size()`) | UNSPECIFIED | Not promised nor excluded by the frozen grammar (member-path method dispatch is not implemented). Documented; do not extend in v4.3. |
| Binding-root method requirement (`[3,1,2].sort()`) | INTENTIONAL_BOUNDARY | Methods require a binding root; inline literals are not method receivers. Documented; do not broaden. |
| Reference values in array literals (`[() => 7]`) | INTENTIONAL_BOUNDARY | Literal grammar is JSON; reference values are stored via `push`/assignment. Documented; do not change the literal grammar. |

CP36 and CP45-CP50 remain not started.

## CP36 certification record (closure ownership/lifetime)

CP36 exercised escaping closure environments, captured binding lifetime,
mutable captured bindings, multiple closures sharing one binding, independent
closure factories, nested/returned closures, closure passed to and composed
through callables, closures stored in arrays/stack/map values, closure->collection
mutation, expression and block lambdas, and repeated creation/destruction,
under ASan/UBSan/LSan and Valgrind (memcheck, definite+possible leaks).

Result: clean. Zero sanitizer findings, zero Valgrind errors, zero leaks across
focused closure workloads, 40 process create/destroy cycles, and the maintained
sanitizer walls (checkpoint-3, checkpoint-4-watch-sanitize, checkpoint-6-sanitize,
checkpoint-9 parser fuzz) plus Valgrind walls (checkpoint-4, checkpoint-6). The
shared binding-slot closure model and escaping environments show no dangling
slots, use-after-free, double-free, or leaks.

No closure-lifetime defect required a production fix. One grammar boundary was
observed (not a memory issue): a struct field can hold a callable and it can be
invoked after rebinding to a local, but a single-line struct definition whose
field initializer is a lambda alongside methods trips the struct field/method
boundary scanner; multi-line definitions work. This is recorded as a grammar
boundary to revisit (potentially with the deferred @script decision), not a
CP36 defect.

## CP45 certification record (complete regression wall)

Full local regression wall on the CP36-certified baseline: `make test` (365
PASS including the wired v4.3 focused tests), focused v4.3/adversarial tests
PASS, independent `nift-regression-suite` 35/35 PASS (historical + v4.1 +
v4.2 + v4.3 black-box contracts), guarantee registry PASS, version
consistency (4.3.0) PASS, `git diff --check` clean. No regression surfaced.

## CP46 certification record (ASan/UBSan/Valgrind/memory)

ASan/UBSan (detect_leaks=1, halt_on_error) and Valgrind (memcheck, definite
+ possible leaks) run clean over a comprehensive v4.3 workload exercising
closures, all six collections (map with int/string/double keys, sorted_map,
set, sorted_set, stack, queue, prique), higher-order operations, expression and
block lambdas, closures as callbacks, int64/double boundaries, and empty
collections. Zero findings, zero Valgrind errors, zero leaks. Maintained walls
pass: checkpoint-3, checkpoint-4-watch-sanitize, checkpoint-6-sanitize,
checkpoint-9 parser fuzz, valgrind checkpoint-4 and checkpoint-6,
pagination-sanitize, and the BH5 parser/value adversarial battery under the
sanitizer. No CP46 defect required a production fix.

## CP47 certification record (formal performance campaign)

Environment: Linux x86_64 (i7-12700H, 20 cores, 64 GB), g++ 15.2.0
`-O2 -std=c++17`, cold `nift build --all` per workload, 15 samples
(min/p25/median/p75/max/mean/stddev recorded in `benchmarks/v43_perf.py`).

Median cold-build times:

```
array_push_10k         0.018s
map_string_5k          0.476s   (set + @for)
map_int_5k             0.211s   (set + @for)
map_int_iter_5k        0.213s   (set + @for)
sorted_map_5k          2.785s
set_5k                 0.205s
sorted_set_5k          0.998s
higher_order_5k        0.091s   (map/filter/reduce over 5k)
fn_invoke_10k          0.039s
lambda_invoke_10k      0.030s
closure_10k            0.031s
```

Map typed-entry iteration: set-only 5k int keys 0.204s vs set+@for 0.202s —
iteration cost is within noise, confirming direct typed-entry traversal with no
per-key stringification or temporary object materialization.

Ordinary-project baseline: the 10,000-page full build measures 0.9996x of the
v4.2 baseline (-0.0%); the v4.3 expression/collection layer adds no
baseline-template overhead.

Recorded characteristics (not changed during CP47, pending the @script
decision and final reconciliation): insertion-ordered map/set use a linear key
scan per insert (O(n^2) build; `fast_map`/`fast_set` remain deferred per the
handover), and `sorted_map`/`sorted_set` perform a per-insert `stable_sort`
(O(n^2 log n); a sorted-position insert would be O(n^2) total) — a recorded
future-optimization candidate, not a correctness regression. No production
code was changed during CP47.

## Native scripting / REPL extension — planned before CP48

The v4.3 language campaign is now extended with a native scripting/REPL pass
before CP48-CP50. This is intentionally one runtime with multiple hosts, not a
second language implementation. Reuse the statement/expression/function/struct/
lambda/closure machinery already certified above. If `@script`, `@import`,
`nift run` or `nift sh` start acquiring independent parsers/evaluators, stop and
refactor around the shared native statement-program abstraction.

### Documentation split: template land vs script land

Keep the existing **Core reference** focused on what is useful when building
websites with Nift: template directives, `$[...]`, metadata, paths/tracked
files, JSON/schema, loops, functions/fragments, structs, expressions and the
collection/value features that are useful in templates.

Document the new execution/I/O/filesystem/stream/REPL surface in a separate
**Scripting** ("script land") section rather than crowding the Core reference.
The conceptual split is:

```text
template land
    normal Nift templates / website generation
    @script { ... } enters inline script land
    @import(path) imports isolated script bindings

script land
    native statements without template @ prefixes or $[...] wrappers
    nift run path executes a script file
    nift sh provides a persistent interactive script environment
```

`@system` / arbitrary subprocess execution remains explicitly out of scope.

### Phase 10 — shared native statement engine

- [ ] **CP51:** freeze the complete native scripting contract: hosts, grammar, scopes, completion/return rules, exports, I/O capabilities, filesystem/path rules, stream/resource semantics, and the template-land/script-land documentation boundary.
- [ ] **CP52:** audit the existing function/block-lambda/control-flow/declaration parser and executor; identify duplicated or host-specific paths before changing behavior.
- [ ] **CP53:** introduce one shared native statement-program parse/execute abstraction with no public behavior change.
- [ ] **CP54:** migrate named function bodies onto the shared statement-program machinery; retain all existing function semantics/tests.
- [ ] **CP55:** migrate block lambda bodies onto the same machinery; preserve lexical binding capture, closure lifetime and return/error behavior.
- [ ] **CP56:** generalize native statement declarations so script land reuses the existing `fn`/`struct` implementations rather than creating script-specific declarations.

### Phase 11 — inline `@script { ... }`

- [ ] **CP57:** implement the `@script { ... }` template grammar boundary. Inside the block use native statements (`fn`, `if`, `for`, `while`, assignments, calls, etc.) without template `@` prefixes or `$[...]` expression wrappers.
- [ ] **CP58:** execute inline `@script` in the current lexical/template scope; declarations and mutations have the same binding semantics as other current-scope language operations.
- [ ] **CP59:** freeze/implement completion semantics: fallthrough -> no rendered output; bare `return` -> early exit/no output; `return expression` -> early exit and render exactly that value. Returned strings are values and are never reparsed as template/Nift source.
- [ ] **CP60:** adversarial inline-script gate: nested control flow, break/continue legality, functions, structs, lambdas/closures, collections, compound assignment, `++/--`, early returns, errors, scope interactions and no internal-reference leakage.

### Phase 12 — external scripts, exports and `@import(path)`

- [ ] **CP61:** add one external native-script file loader/executor shared by `@import` and `nift run`; do not implement separate interpreters.
- [ ] **CP62:** external/imported scripts execute in an isolated lexical environment with no implicit access to caller bindings.
- [ ] **CP63:** implement native script-land `export(binding)` (no `@`). The named binding must already exist; export the binding itself, not a value snapshot.
- [ ] **CP64:** certify exported binding/closure lifetime: exported closures retain private script bindings; exporting a binding plus a closure that captures it exposes the same live binding.
- [ ] **CP65:** make import export installation atomic: execute privately, collect/validate all exports, reject missing/duplicate exports and caller-name collisions, then attach all bindings or none.
- [ ] **CP66:** implement `@import(path)` as isolated script execution plus atomic exported-binding installation; the directive itself renders nothing.
- [ ] **CP67:** import completion contract: fallthrough -> successful import; bare `return` -> early successful import; `return expression` -> deterministic error. `export()` is the programmatic interface; `print()` is only an output side effect.
- [ ] **CP68:** integrate import path/security/dependency semantics with Nift's existing project/path rules and tracked/incremental dependency model, including applicable transitive imports.

### Phase 13 — remaining array/string ergonomics

- [ ] **CP69:** add/certify `array.join(separator)`, `array.slice(start)`, `array.slice(start,end)`, `array.splice(start,deleteCount)`, `array.splice(start,deleteCount,replacements)` and `array.reverse()`. `slice` is non-mutating; `splice`/`reverse` mutate. Freeze negative/out-of-range/index and replacement semantics explicitly.
- [ ] **CP70:** add/certify `text.substr(pos)` and `text.substr(pos,length)` and script string composition. Existing quoted `$[...]` interpolation remains available inside strings (for example `print("hello, $[name]")`), while ordinary script expressions use `print(name)` rather than `print($[name])`. Keep deliberate string concatenation semantics without JS-style coercive equality/comparison.

### Phase 14 — filesystem primitives and whole-file input

- [ ] **CP71:** implement `pwd()`, `cd(path)`, `exists(path)`, `copy(source,destination)`, `move(source,destination)`, `remove(path)`, `make_dir(path)` and `touch(path)`. Use the readable names (`copy`/`move`/`remove`) rather than `cp`/`mv`/`rm`.
- [ ] **CP72:** filesystem safety/path gate: define current-directory behavior for build hosts, `nift run` and `nift sh`; preserve existing Nift path/security constraints; do not make `remove()` an accidental unrestricted recursive-delete primitive.
- [ ] **CP73:** implement `open(path)` as whole-file data input, initially returning the complete file as a string. Keep it semantically distinct from `@input`, `@import` and source injection (`inject`).

### Phase 15 — file streams

- [ ] **CP74:** introduce a proper runtime stream/resource value with identity, valid/closed state and safe non-renderability; no internal handle/reference leakage.
- [ ] **CP75:** implement input streams: `ifs(path)`, `stream.read(n)`, `stream.readLine()`, `stream.readAll()`, `stream.eof()` and `close(stream)`.
- [ ] **CP76:** implement `stream.read_val()`: consume one serialized Nift data value as if interpreting the RHS of `x := ??` (including supported `null`, bool, int, double, string, array/object/data values). Reuse the Nift value parser; freeze whitespace/value-boundary/malformed-input/EOF behavior and do not use target-variable typing.
- [ ] **CP77:** implement output streams with `ofs(path)` plus explicit `write(value)`, `writeLine(value)` and `flush()` operations and `close(stream)`. Freeze create/truncate/write failure semantics; defer `<<`/`>>` operator sugar.
- [ ] **CP78:** stream ownership/adversarial gate: large files, repeated reads/writes, EOF, malformed `read_val`, explicit/double close, operation-after-close, automatic cleanup on errors, escaping stream values and sanitizer/leak behavior.

### Phase 16 — console I/O

- [ ] **CP79:** implement `print(expression)` with exactly one expression initially, normal safe Nift value rendering and a trailing newline. `print("hello, $[var]")` and `print("hello, " + var)` must both work according to the normal string/interpolation rules.
- [ ] **CP80:** make each `print()` invocation an atomic blocking console write: evaluate/render first, then serialize the complete line through the shared output sink so concurrent Nift execution cannot interleave fragments of two prints.
- [ ] **CP81:** implement `read()` as one input line with the line terminator removed; EOF -> `null`; I/O failure -> runtime error. Interactive stdin is available to `nift run`/`nift sh`; ordinary website builds must not unexpectedly block waiting for stdin. `print()` remains a process/console side effect, not template rendering.

### Phase 17 — `nift run path`

- [ ] **CP82:** implement `nift run path` using the shared external script executor in a fresh root environment. Fallthrough/bare return -> exit 0/no return output; `return expression` -> render the result to stdout and exit 0; `print` -> stdout; `read` -> stdin; parse/runtime/I/O error -> diagnostic on stderr and non-zero exit. No temporary Nift project/build should be required internally.
- [ ] **CP83:** `nift run` adversarial/CLI gate: piped stdin, EOF, stdout/stderr separation, exit status, functions/structs/lambdas/closures/collections, imports, filesystem/streams, working-directory semantics, and the same file being both importable and runnable. `export()` may validate/record exports under `run` but has no importer/consumer.

### Phase 18 — `nift sh` persistent interpreter

- [ ] **CP84:** restore `nift sh` as a persistent native-Nift REPL using the exact same statement engine; retain one root lexical environment between commands and display `pwd()` as the prompt. `cd(path)` updates the prompt naturally.
- [ ] **CP85:** multiline REPL parsing for functions, structs, lambdas, loops and conditionals. Use parser incomplete-input state rather than naive brace counting.
- [ ] **CP86:** REPL error recovery: ordinary syntax/runtime errors report cleanly without corrupting or terminating the persistent environment.
- [ ] **CP87:** REPL I/O interaction: `read()` temporarily consumes program input and returns control to the REPL; `print()` keeps atomic output semantics.
- [ ] **CP88:** long-running REPL lifetime/resource gate: persistent closures, structs, collections, imported bindings, streams, repeated commands, `cd()`, errors and cleanup remain correct under sanitizer/Valgrind where available.

### Phase 19 — scripting certification and dogfood

- [ ] **CP89:** expand the independent `nift-regression-suite` with black-box coverage for `@script`, `@import`/exports, array/string additions, filesystem operations, whole-file/stream I/O, console I/O, `nift run` and `nift sh`; preserve historical contracts.
- [ ] **CP90:** full scripting sanitizer/memory/resource wall: ASan/UBSan/LSan/Valgrind where available, especially exported bindings/closures, stream resources, failed imports and persistent REPL environments.
- [ ] **CP91:** formal scripting performance campaign: statement execution, `nift run` startup, repeated REPL statements, function/lambda invocation, imports, filesystem operations, `open`, `ifs`/`ofs` streaming and `read_val`; compare against the already-recorded v4.3 baseline rather than optimizing blindly.
- [ ] **CP92:** agent-driven `nift sh` dogfood: have an agent use one persistent shell for representative scripting/data-transformation tasks and record whether reduced process startup/reparse overhead or persistent state materially helps. Treat this as ergonomic/product evidence, not a benchmark designed to prove a predetermined result.
- [ ] **CP93:** complete native-scripting contract wall: all historical Nift tests + v4.3 tests + independent regression suite + website build/checks + version/guarantee checks + `git diff --check`; reconcile this handover and STOP before CP48-CP50.

After CP93, return to the existing final sequence:

```text
CP48  final repository/documentation reconciliation
CP49  final website certification/publication preparation
CP50  independent release-candidate/provenance audit
STOP before tag/push/release/publication
```
