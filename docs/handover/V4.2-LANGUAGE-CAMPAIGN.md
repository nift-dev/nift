# Nift v4.2.0 Language Campaign

## Frozen contract

v4.2 separates template grammar from `@fn` program grammar.

| Template | Function body |
|---|---|
| `@if(...) {...}` | `if(...) {...}` |
| `@for(...) {...}` | `for(...) {...}` |
| `@while(...) {...}` | `while(...) {...}` |
| `$[x := expr]` | `x := expr` |
| `$[x = expr]` | `x = expr` |
| `$[foo(...)]` | `foo(...)` |
| `break`, `continue` in loops | `break`, `continue` in loops |
| bare `return` in fragments | `return [expr]` |

Function statements accept newline, `;`, and block close as boundaries. `@fn` may return a value or no value; no value is `null`. Fragments remain template grammar and may only use bare value-less `return`. `break` and `continue` target the innermost `@for`/`@while` in the current callable boundary and cannot escape a function/fragment call.

`null` is a first-class scalar. Equality/inequality with null are valid; ordering with null is an error.

`copy` is reserved for shallow copying and `deepcopy` for recursive copying in the struct tranche after CP14.

## Checkpoints

Status blocks are appended below. Each checkpoint records commit, semantics, tests, gates, performance, rejected alternatives, and next checkpoint.

### CP1
Commit: `63ea62e` (campaign contract; amended status metadata is part of the same checkpoint)
Semantics changed: none; v4.2 grammar and contextual validity frozen.
Tests added: none.
Gates run: documentation/diff hygiene.
Performance result: not applicable.
Rejected alternatives: making legacy `@return(...)` the canonical return form (bare `return` is canonical; legacy `@return(...)` is retained only as v4.1 compatibility and is the mechanism canonical `return` translates to); function bodies remaining template grammar; deep `copy` on assignment.
Next checkpoint: CP2 generic abrupt-control propagation.

### CP2
Commit: this checkpoint commit.
Semantics changed: none; return-specific pending state generalized to `ControlFlow`/`PendingControl`.
Tests added: existing v4.1 adversarial return wall reused.
Gates run: build; v4.1 certification adversarial.
Performance result: structural refactor only; no expected hot-path change.
Rejected alternatives: three independent pending booleans.
Next checkpoint: CP3 first-class null certification.

### CP3
Commit: this checkpoint commit.
Semantics changed: `null` certified as first-class scalar; equality/inequality supported, ordering rejected (implementation already present in the v4.2 base).
Tests added: `tests/v42_null_smoke.sh`.
Gates run: focused null smoke.
Performance result: no implementation change.
Rejected alternatives: ordering null; separate void sentinel.
Next checkpoint: CP4 function statement grammar.

### CP4
Commit: this checkpoint commit.
Semantics changed: `@fn` bodies now pass through a statement layer; bare declaration/assignment/call expressions accept newline or optional semicolon boundaries without `$[...]`.
Tests added: `tests/v42_function_program_smoke.sh`.
Gates run: build; focused function-program smoke.
Performance result: translation occurs only on function invocation; baseline templates unaffected.
Rejected alternatives: separate expression implementation; mandatory semicolons.
Next checkpoint: CP5 function if/else grammar.

### CP5
Commit: this checkpoint commit.
Semantics changed: function program grammar supports `if` / `else if` / `else` with lexical branch scopes via existing template control evaluator.
Tests added: focused branch coverage folded into function-program smoke path.
Gates run: build; focused three-way function branch probe.
Performance result: function-only translation path.
Rejected alternatives: requiring `@if` inside functions.
Next checkpoint: CP6 function `for`.

### CP6
Commit: this checkpoint commit.
Semantics changed: function program grammar supports bare `for(...) {...}` and reuses v4.1 live-iteration/scoping semantics.
Tests added: translator/build coverage.
Gates run: build.
Performance result: no baseline-template path change.
Rejected alternatives: separate function-loop evaluator.
Next checkpoint: CP7 template `@while`.

### CP7
Commit: this checkpoint commit.
Semantics changed: template grammar adds `@while(condition){...}` with normal inherited mutation and per-iteration scope; no artificial execution budget.
Tests added: `tests/v42_while_smoke.sh`.
Gates run: build; focused while smoke.
Performance result: unused unless `@while` appears.
Rejected alternatives: arbitrary iteration/fuel ceiling.
Next checkpoint: CP8 function `while`.

### CP8
Commit: this checkpoint commit.
Semantics changed: function program grammar adds bare `while(condition){...}`, sharing template while evaluation semantics.
Tests added: translation/build coverage; template while smoke retained.
Gates run: build; while smoke.
Performance result: function-only translation addition.
Rejected alternatives: separate while runtime.
Next checkpoint: CP9 bare `continue`.

### CP9
Commit: this checkpoint commit.
Semantics changed: bare `continue` is valid only in the innermost current loop; callable entry resets loop ownership so a callee cannot continue its caller.
Tests added: `tests/v42_continue_smoke.sh`.
Gates run: build; focused continue smoke.
Performance result: one cheap contextual token check in parse loop.
Rejected alternatives: `@continue`; cross-callable loop control.
Next checkpoint: CP10 bare `break`.

### CP10
Commit: this checkpoint commit.
Semantics changed: bare `break` exits only the innermost current loop; callable boundaries block caller-loop escape.
Tests added: `tests/v42_break_smoke.sh`.
Gates run: build; focused break smoke.
Performance result: one cheap contextual token check.
Rejected alternatives: `@break`; labelled break in v4.2.
Next checkpoint: CP11 bare function return.

### CP11
Commit: this checkpoint commit.
Semantics changed: function program grammar adds bare `return expr` as the canonical v4.2 return statement; it translates to the existing return-capture path. Legacy `@return(...)` remains accepted inside `@fn` bodies for compatibility with v4.1 function programs (it is the same capture mechanism bare `return` translates to and remains the documented v4.1 spelling), but `return expr` / bare `return` are the recommended v4.2 forms. Outside function bodies `@return(...)` stays template passthrough.
Tests added: `tests/v42_return_smoke.sh` with nested conditional return.
Gates run: build; focused return smoke.
Performance result: function-only statement translation.
Rejected alternatives: removing legacy `@return(...)` capture (would break canonical `return`, which is translated to it); a separate return bytecode path.
Next checkpoint: CP12 no-value function/null semantics.

### CP12
Commit: this checkpoint commit.
Semantics changed: bare `return` and function fallthrough produce first-class `null`; functions may optionally return values.
Tests added: no-value explicit return and fallthrough-null cases.
Gates run: build; return/null smoke.
Performance result: no meaningful hot-path change.
Rejected alternatives: separate `void` sentinel; mandatory function return.
Next checkpoint: CP13 fragment early return.

### CP13
Commit: this checkpoint commit.
Semantics changed: fragments retain template grammar but gain bare value-less `return`; rendered prefix is retained; `return expr` is rejected.
Tests added: `tests/v42_fragment_return_smoke.sh`.
Gates run: build; fragment early-return smoke and invalid value-return case.
Performance result: contextual token check only while parsing fragment bodies.
Rejected alternatives: fragment `@return`; fragment value channel; transactional rollback.
Next checkpoint: CP14 adversarial/certification wall.

### CP14
Commit: this checkpoint commit.
Semantics changed: no new syntax; v4.2 control-flow core certified across nested function/template/fragment contexts and callable boundaries.
Tests added: `tests/v42_control_adversarial.sh`; historical v4.1 adversarial callable cases updated to current v4.2 function grammar and optional-return semantics.
Gates run: build; all focused v4.2 null/while/continue/break/return/fragment tests; control-flow, template-variable, cross-feature and certification adversarial targets. Full `make test` was attempted and reached the existing pagination-ordering gate, which exits 77 when `strace` is unavailable in this environment; no product failure preceded that environmental stop.
Performance result: baseline template path gains only contextual token checks; no benchmark regression measured in CP14. Dedicated performance certification remains later in the v4.2 campaign.
Rejected alternatives: weakening callable boundaries; retaining obsolete mandatory-return v4.1 test expectations.
Next checkpoint: CP15 struct contract design (not started in this requested tranche).

## Struct tranche contract (CP15-CP27)

### CP15
Commit: this checkpoint commit.
Semantics changed: none; struct contract frozen before implementation.
Tests added: none.
Gates run: documentation consistency review.
Performance result: no runtime change.
Rejected alternatives: value semantics; implicit/dynamic fields; multiple constructors; inheritance/class hierarchy; implicit deep copies.
Next checkpoint: CP16 struct definitions.

Frozen contract:
- `@struct(name) { ... }` defines a fixed-shape user type and emits no output.
- fields are declared with `name := initializer`; an initializer is mandatory and fixes the field type.
- fields and methods are public by default; `private` may qualify either.
- methods use function-program grammar: `fn(name(args)) { ... }`; at most one constructor is allowed and is spelled `fn(struct_name(args)) { ... }`.
- instances have reference semantics. Assignment/passing/return aliases the same instance.
- `copy(x)` is an explicit shallow copy; `deepcopy(x)` recursively copies mutable aggregate state.
- fields/methods are implicitly visible inside methods; `this` explicitly denotes the receiver.
- no dynamic field addition/removal, inheritance, protected, overloads, destructors, generics, operator overloading or implicit JSON serialization in v4.2.
- methods may return a value, `return` without a value, or fall through; no-value results are `null`.
- constructor return values are forbidden.

### CP16
Commit: this checkpoint commit.
Semantics changed: template grammar parses fixed-shape `@struct(name){...}` definitions with initialized fields and method/constructor declarations; definitions emit no output.
Tests added: `tests/v42_struct_definition_smoke.sh`.
Gates run: build; focused definition smoke.
Performance result: struct parser is entered only when `@struct` occurs.
Rejected alternatives: dynamic fields; uninitialized fields.
Next checkpoint: CP17 instances/construction.

### CP17
Commit: this checkpoint commit.
Semantics changed: struct names are constructible expressions; each construction creates fresh instance storage and struct values carry reference identity through ordinary binding assignment/passing.
Tests added: `tests/v42_struct_instance_smoke.sh`.
Gates run: build; focused construction/alias smoke.
Performance result: instance allocation only on explicit construction.
Rejected alternatives: value-copy-on-binding semantics.
Next checkpoint: CP18 field access/mutation.

### CP18
Commit: this checkpoint commit.
Semantics changed: fixed struct fields support member reads and mutation; aliases observe the same instance; unknown fields and type-changing assignments fail.
Tests added: `tests/v42_struct_fields_smoke.sh`.
Gates run: build; alias/member/fixed-shape smoke.
Performance result: member lookup is bounded hash lookup after reference resolution.
Rejected alternatives: dynamic field creation; type-changing fields.
Next checkpoint: CP19 constructor execution.

### CP19
Commit: this checkpoint commit.
Semantics changed: one same-name constructor may initialize a fresh instance after field defaults; constructor parameters use program grammar and explicit value returns are rejected.
Tests added: `tests/v42_struct_constructor_smoke.sh`.
Gates run: build; constructor initialization smoke.
Performance result: constructor reuses callable/program evaluator.
Rejected alternatives: `init`; multiple/overloaded constructors.
Next checkpoint: CP20 methods.

### CP20
Commit: this checkpoint commit.
Semantics changed: public instance methods execute the same function-program grammar as top-level functions, with implicit field bindings and persistent receiver mutation; methods may return values or null.
Tests added: `tests/v42_struct_methods_smoke.sh`.
Gates run: build; stateful method smoke.
Performance result: method execution reuses function translation/evaluation path.
Rejected alternatives: separate method language/runtime.
Next checkpoint: CP21 `this` and shadowing.

### CP21
Commit: this checkpoint commit.
Semantics changed: method lookup precedence is local/parameter then implicit instance field then outer scope; `this` explicitly names the receiver and disambiguates shadowed fields.
Tests added: `tests/v42_struct_this_smoke.sh`.
Gates run: build; parameter-shadow/explicit-this smoke.
Performance result: `this` is a normal receiver reference binding.
Rejected alternatives: mandatory `this`/`self` qualification.
Next checkpoint: CP22 privacy.

### CP22
Commit: this checkpoint commit.
Semantics changed: `private` applies to fields and methods; receiver methods may access private members while external member access/calls are rejected. Public remains the default; no protected/inheritance tier exists.
Tests added: `tests/v42_struct_private_smoke.sh`.
Gates run: build; internal/private access and external rejection smoke.
Performance result: one definition metadata check on member access.
Rejected alternatives: class/public/private sections; protected/friend semantics.
Next checkpoint: CP23 shallow copy.

### CP23
Commit: this checkpoint commit.
Semantics changed: `copy(x)` explicitly creates a distinct shallow struct instance; field storage is new but nested reference-valued fields retain their referenced identities. Non-struct values retain ordinary value-copy behavior.
Tests added: `tests/v42_struct_copy_smoke.sh`.
Gates run: build; shallow independence smoke.
Performance result: O(number of fields), with no recursive traversal.
Rejected alternatives: implicit copy on assignment; unexpectedly recursive `copy`.
Next checkpoint: CP24 `deepcopy`.

### CP24
Commit: this checkpoint commit.
Semantics changed: `deepcopy(x)` recursively duplicates struct instances and mutable JSON arrays/objects, preserving graph sharing/cycles through an old-to-new identity map; private fields participate because copying is runtime state duplication, not serialization.
Tests added: `tests/v42_struct_deepcopy_smoke.sh`.
Gates run: build; nested-struct recursive independence smoke.
Performance result: recursive cost is explicit and proportional to reachable mutable graph size.
Rejected alternatives: making `copy` recursive; serialization-based copying.
Next checkpoint: CP25 struct adversarial certification.

### CP25
Commit: this checkpoint commit.
Semantics changed: no new surface syntax; struct semantics certified together, including implicit method dispatch inside methods, constructor state, private access, function pass/return aliasing, `this`, shallow copy and recursive deepcopy.
Tests added: `tests/v42_struct_adversarial.sh`.
Gates run: build; all CP16-CP24 struct smokes; integrated adversarial struct program.
Performance result: no new baseline-template work outside struct/member detection.
Rejected alternatives: weakening receiver/private boundaries to simplify dispatch.
Next checkpoint: CP26 independent regression-suite expansion.

### CP26
Commit: Nift handover checkpoint plus regression-suite commit `ccab482`.
Semantics changed: no production syntax change; independent black-box contract advanced to v4.2 callable semantics and struct guarantees.
Tests added: regression-suite `contract/v42_structs_smoke.sh`; v4.1 executable-current callable modules updated for v4.2 return/fallthrough behavior.
Gates run: focused v4.2 struct contract; updated v4.1 language and adversarial contract modules.
Performance result: no production change.
Rejected alternatives: leaving the independent suite pinned to obsolete `@return`/mandatory-return behavior.
Next checkpoint: CP27 website/language-reference update.

### CP27
Commit: Nift handover checkpoint; website rendered-public `a81dfca`; website source/stage `3ac938d`.
Semantics changed: no production syntax change; public language documentation now distinguishes template grammar from function-program grammar and documents null/optional returns, while/break/continue, structs, privacy, reference semantics, `this`, `copy` and `deepcopy`.
Tests added: documentation build is the acceptance surface.
Gates run: website full build with the CP25 Nift binary; `nift status`; rendered-public/source commit hygiene.
Performance result: no runtime change.
Rejected alternatives: documenting structs as JSON aliases; presenting canonical function-body syntax as v4.1 `@return(...)` (docs show `return` as the canonical v4.2 form while legacy `@return(...)` remains accepted for compatibility).
Next checkpoint: CP28 performance/memory certification (not started in this requested tranche).

### Deferred work (recorded after the pre-CP28 numeric-typing checkpoint)

The following are NOT implemented in v4.2 and are intentionally deferred beyond this release:

- **General collection mutation.** Nift has no in-place array element assignment, object member assignment, or `push`/`pop`/`append`/`remove` API. Array/object values are constructed from literals or value-producing transforms (`@filter`, `@map`, `@sort`, `@slice`, etc.); a binding is updated by assigning another value of the same type.
- **Structs inside arrays/objects.** Because the language has no element/member assignment surface, a struct instance cannot practically be stored into or mutated through an array or object. `copy()`/`deepcopy()` retain their existing semantics for aggregate values the language can actually construct; the `deepcopy` array/object recursion applies to mutable JSON aggregates, not to structs reached through a container mutation surface that does not exist.
- **Numeric JSON classification.** Parsed JSON numbers keep their established double-value representation; only Nift source literals are typed by lexical form (`0`/`8` int; `0.0`/`8.0`/`8.5`/`1e3` double). No JSON numeric semantics changed.

These are candidates for a later collection-language campaign, not defects in the implemented v4.2 contract.

### CP28
Commit: `b3e4d97` (evidence), `97cad1c` (benchmark harness), `6944d73` (function-program translation passthrough), `1732185` (struct member expressions).
Semantics changed: no v4.2 language change; the only semantic-adjacent fix was that template-grammar statements (`@`-directives, `$[...]`) pass through function-program translation verbatim instead of being double-wrapped (restores v4.1 direct-parse behavior for legacy bodies; bare v4.2 statements unchanged).
Tests added: `tests/v42_struct_member_expression_smoke.sh` (struct member-path arithmetic), wired into `test-v42-struct`; `benchmarks/cp28_fixtures.py` + `benchmarks/cp28_bench.py`; evidence under `docs/evidence/cp28/`.
Gates run: full CP28 performance/memory certification (per-workload timings, 10k A/B audit, callgrind attribution, ASan/UBSan/LSan on every workload, Valgrind targeted + maintained walls, struct-instance retention measurement).
Performance result: ordinary-project baseline overhead +0.4% (10k full build) / +1.1% (render-only); legacy v4.1 function calls regressed +77% and were restored to within noise; v4.2 feature workloads all sub-16ms single-page; struct retention linear (50k->31.5MB, 100k->58.6MB, 200k->112MB), bounded by page-build lifetime, no cross-page retention.
Rejected alternatives: caching the function-program translation (not the bottleneck); adding `a.v[0]` struct array-field indexing (deferred semantics).
Open questions: none blocking. Per-workload fixtures are process-startup-dominated and cannot resolve sub-10% deltas; the 10k audit is the reliable baseline signal.
Next checkpoint: CP29 full cross-platform/hardening wall.

### CP29
Commit: this handover checkpoint; BH3 mutation evidence regenerated (`docs/evidence/bh3/bh3-mutation-tranche1.json`).
Semantics changed: none. No language or semantics changes; no deferred features implemented.
Tests added: none required (existing walls).
Gates run: `make test` (365 PASS); independent contract (34/34); sanitizers (checkpoint-3, checkpoint-4-watch-sanitize, checkpoint-6-sanitize, checkpoint-9 parser fuzz 1217 cases); Valgrind (checkpoint-4 30 cycles flat RSS, checkpoint-6, targeted struct/copy/deepcopy/method memcheck); fuzzing (checkpoint-9, parser-value-composition, crash-recovery); incremental equivalence (checkpoint-7, BH4 state transitions, pagination equivalence); package consumers (`make test-embed`, `make test-bindings` Go/C#/Node/Python, `matrix-build.sh linux-x86_64 --require-smoke`, installer smoke); cross-platform (macos-runner-policy 10 entries, checkpoint-10 corpus 18 cases, build-boundary + nondestructive, version-derivation); dependency sync (jsonic/minify checkpoint-6-sync, markuppp vendored content diff clean); website build/checks (78 files, 9440 links 0 missing); performance gate (10k +0.2%, per-workload within noise); BH2 test-integrity chain; guarantee registry + bh1 liveness; `make test-all`.
Performance result: no material regression; 10k full-build +0.2% vs baseline; CP28 evidence preserved.
Rejected alternatives: none (no feature work).
Open questions: BH3 guard-mutation tranche 1 reclassifies the modified-mode stub case from LIVE_FALSE_GREEN to HYPOTHESIS_REJECTED — direct confirmation that the CP29 (modified-mode equal-timestamp) fix removed the false-green the stub previously exposed. macOS/Windows native execution is CI-gated; local coverage is the Linux corpus plus the repository's cross-platform consistency guards.
Next checkpoint: CP30 v4.2 development freeze / release-readiness (not started).

### CP30
Commit: `2b7e614` (v4.2.0 release notes + current-state reconciliation).
Semantics changed: none. No production code, public semantics, or tests changed (production-code diff since CP29: none). Documentation/release-metadata reconciliation only.
Tests added: none.
Gates run: `make test` (365 PASS); independent contract (34/34); version consistency (executable=4.2.0, Snap=4.2.0); dependency synchronization; guarantee registry; package/version derivation; website build/checks (no website content change); `git diff --check` clean.
Performance result: not re-run (no production code change since CP29; CP29 froze and certified the implementation).
Rejected alternatives: none (no feature or semantic work).
Open questions: none. Frozen candidate `59e61ef`; post-push native macOS/Windows CI and the full post-push release gates remain release-authorization prerequisites, not CP30 failures.
Next checkpoint: none. v4.2 development freeze complete; release authorization is a separate decision after review.
