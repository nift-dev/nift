# Nift decision ledger

This ledger distinguishes decisions from ideas discussed historically. Update
statuses when new evidence changes a conclusion and retain useful superseded
rationale. “Rejected” means good reasons exist, not that future evidence is
forbidden.

## Product terminology

**Status:** SETTLED

Describe Nift generally as a website generator, not merely a static site
generator. Generated websites may contain runtime applications and dynamic
frontend behavior.

**Revisit if:** the product identity changes materially.

## Glue, not universe

**Status:** SETTLED DIRECTION

Nift owns dependency-aware build-time composition. Ordinary web technologies,
source compilers, package managers, backends, and task orchestration remain
external and composable.

**Revisit if:** repeated real projects demonstrate that external composition for
a fundamental generation concern is consistently more complex than a small Nift
primitive.

## General scripting and arbitrary hooks

**Status:** REJECTED

Do not casually restore LuaJIT, ExprTk, mutable template programming, system
execution, or arbitrary pre/post hooks. Their removal is foundational to modern
Nift.

**Revisit if:** a substantial project family cannot express necessary build-time
rendering with constrained primitives and external orchestration remains clearly
worse.

## Structured data and control flow

**Status:** ACCEPTED/CURRENT

JSON, Schema validation, constrained loops, conditions, stable sorting, and
lexical scope belong because they directly support deterministic rendering.
They do not imply a general expression runtime.

## Unknown web `@` syntax

**Status:** DELIBERATE CURRENT BEHAVIOR

Unknown constructs such as CSS at-rules pass through. Nift is strict about syntax
it recognizes while remaining transparent to ordinary web text.

## Quotes

**Status:** SETTLED/CURRENT

Single and double quotes are Nift parameter quotes. Backticks are not.

## Dependencies versus requirements

**Status:** CORE MODEL

Dependencies can affect generated bytes. Requirements mean generated output
assumes a path exists. Preserve this distinction in incrementality and docs.

## Correctness versus extra rebuilds

**Status:** SETTLED PRIORITY

A missed rebuild is a correctness defect; an extra rebuild is a performance
problem. Choose conservative correctness first.

## Persisted state trust

**Status:** REJECTED ASSUMPTION

Never trust state solely because Nift wrote it. Validate structure and semantics.

## Filesystem containment

**Status:** SETTLED

Use semantic filesystem containment. String-prefix containment is rejected.

## Parameter interpolation

**Status:** SETTLED/CURRENT

Textual directive arguments should accept direct `$[...]` values and quoted
literal-plus-value interpolation. Argument boundaries come from source; resolved
data is not reparsed. Interpolation is one pass and side-effect-free. Arbitrary
`@...` operations and dynamic binding identifiers remain outside the feature.

Eligible textual positions are `@input`, every `@dep` argument,
`@pathto`/`@pathtofile`, `@getenv`, `@ent`, and the `@json` source and optional
schema. The `@json` binding identifier, `@content`, `@if`, and `@for` grammar are
not interpolated. Scalars become text; arrays, objects, malformed expressions,
and unknown roots fail through controlled parser errors. `\$` preserves a
literal dollar in quoted parameters.

**Revisit if:** a real use case demonstrates the need for a richer coherent value
model. Do not respond by recursively evaluating full Nift templates in arguments.

## Framework-specific directives

**Status:** REJECTED CURRENTLY

Do not add `@react`, `@vue`, `@svelte`, or `@vite` merely to advertise support.
Ordinary composition has worked.

## Nift as task runner/package manager

**Status:** REJECTED

Use Make, npm, CI, or task runners for external orchestration.

## Minification

**Status:** SETTLED

Minification is explicitly opt-in and applied at the final-output boundary.
Standalone Minify++ retains independent identity and canonical responsibility.

## Performance optimization

**Status:** SETTLED PRINCIPLE

Performance is a product value but may not weaken correctness or safety. Measure
CPU, scaling, peak memory, and lifetimes. The compact staged v1.0.42 validation
architecture has historical rationale and should not be replaced casually.

## External contract suite

**Status:** SETTLED ARCHITECTURE

Keep it implementation-independent. A different implementation should be able to
pass without sharing C++ internals.

## Rust implementation

**Status:** FUTURE POSSIBILITY

An independent implementation against a mature behavioral contract may be
interesting. It is not current maintenance work and is not authorization for a
rewrite.

## Production and public actions

**Status:** SETTLED WORKING RELATIONSHIP

Prepare and validate locally. Pushes, tags, releases, deployments, public version
changes, and destructive restructuring require explicit approval.
