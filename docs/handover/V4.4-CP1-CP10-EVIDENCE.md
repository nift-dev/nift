# v4.4 CP1–CP10 evidence

Implemented through CP10 on 2026-09-19.

Focused source wall:
- CP2 variadic named functions
- CP3 variadic lambdas/callable parity
- CP4 spread for user/lambda/native calls
- CP5 generalized min/max and filesystem multi-source operations
- CP6/7 deterministic filesystem globs
- CP8 scoped object-key wildcards for pick/omit
- CP9 map-to-rendered-markup block lambdas
- CP10 standalone filesystem authority
- existing v4.3 scripting wall and native-I/O smoke remain green

Independent suite: `contract/v44_language_foundation_smoke.sh` was added and wired into `run-contract.sh`; the new module passes against the candidate binary. A full independent contract run was started in this environment but exceeded the 120-second execution window, so this checkpoint does not falsely record a complete-suite pass.

Website: source docs for functions/fragments and scripting filesystem were updated, rebuilt with the candidate binary (94 files, ~0.058 s), and committed in both source and generated-site repositories.

Cross-platform behavior is designed around `std::filesystem` normalization and platform-neutral contracts; Linux is locally exercised here. macOS/Windows remain CI evidence, not locally claimed evidence.
