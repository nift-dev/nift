# v4.4 DeepSeek review / unfinished-work ledger

Review all CP1-CP30 changes before continuing. Do not assume a checkpoint commit means release certification is complete.

## Independent review/repair pass (2026-09-19)

An independent adversarial review + dogfood + repair pass covered CP1-CP40
(see the review report for the full record). Everything below reflects the
post-repair state at commit HEAD of `main` unless stated otherwise.

### Regressions found and fixed (all committed with regressions)

- **`copy(value)` value-copy regression (CP5).** Generalizing copy/cp/move/mv
  to filesystem operations intercepted the v4.3 single-argument value copy of
  collections/structs, breaking existing templates/scripts and the v42/v43
  source + independent regression walls (4 independent modules failed). The
  filesystem branch now only claims copy with 2+ arguments.
- **`pick()` ordering regression (CP8).** Structured-key wildcards changed
  pick result ordering from the requested pattern order to object order. pick
  now walks requested patterns first, restoring requested-order semantics while
  retaining wildcard matching.
- **`@import` exported-callable closure (CP30/CP34).** Exported `@fn` callables
  could not reference the module's private callables or top-level bindings, so
  the sqlite package (and any package with internal helper functions) failed
  with "undefined callable". Exported callables now carry a shared module
  environment (callables + top-level bindings) active while their bodies run,
  preserving import isolation.
- **`--no-process` (CP27).** The documented CLI flag was rejected by `nift
  run`/`nift sh`; only the NIFT_NO_PROCESS environment variable worked. The
  flag is now parsed, and the structured `cmd(...).run()` pipeline path (which
  bypassed the restriction entirely) now denies process execution too.
- **SQLite package (CP35-CP37).** `sqlite_literal` checked `type() ==
  "number"` but `type()` returns "int"/"float", so numeric parameters never
  bound; `inject(variable)` only accepted a quoted literal (fixed in Nift
  core); the query temp file had a fixed name (now `mktemp`); transactions
  used a single BEGIN/COMMIT batch that committed partial work on error (now
  `sqlite3 -bail`, which rolls back atomically); empty result sets produced an
  empty temp file (now returned as `[]`).
- **Shell state (CP14/CP15).** `x = 7` and compound assignments were routed to
  command land and silently attempted to exec a program named `x`; lines whose
  second token is a standalone assignment operator now route to the Nift
  statement engine. The tokenizer split an adjacent fd-redirect such as
  `2>err.txt` into a literal `2` argument plus a stdout redirect; `2>`,
  `2>>`, `2>&1` now tokenize as fd-redirects.
- **inject() path evaluation.** `inject` only accepted a quoted literal path;
  non-literal arguments are now evaluated as string expressions, and the
  standalone-path containment check is relaxed to match the CP10
  filesystem-authority model.
- **Parser mutation-candidate heuristic.** `=`/`:=` inside quoted strings
  (e.g. SQL `WHERE x = ?`) were treated as assignments, breaking postfix
  composition on calls such as `fn("a = b").member`. Assignment operators now
  only count at bracket/paren/quote depth zero.
- **Test wiring.** `v44_shell_glob`, `v44_cp21_history`, `v44_cp22_completion`,
  `v44_cp27_restricted`, `package_refs`, `package_sqlite_dogfood` were not
  wired into the Makefile. They now run under `make test-v44` (and the core
  shell/language/restriction targets join `make test`). Fixed the
  `$OLDPWD/$NIFT` absolute-path bug in three tests, the missing `sh`
  subcommand in the shell-glob test, and its printf format.
- **Highlighter (CP28).** The website grammar treated `//` and `#` as Nift
  comments, but Nift comments are `@//` and `@/* ... */`; it now matches the
  real forms. `check_syntax_highlight.py` was expanded from the @path family
  to the full modern grammar with rule-scoped false-positive checks.

### Verified sound (no change)

- Variadics/spread: user functions, lambdas, fixed+variadic, recursion,
  closures, diagnostics, empty/heterogeneous args, non-array spread rejection.
- Filesystem globs (`*`, `?`, `**`), deterministic ordering, dotfile rules,
  no-match semantics per the CP6 contract, object-key wildcard matching kept
  separate from filesystem globbing.
- map-to-markup block lambdas (rendered vs non-rendered, nested control flow,
  empty arrays, composition).
- Process core: direct fork/execvp (no shell), real OS pipes for intermediate
  pipeline stages, capture via temp files, exit codes/stdout/stderr, PATH
  lookup, env inheritance, large output, `-bail` behavior.
- Command resolution precedence (Nift builtins > PATH), `which`, native
  aliases (cp/mv/rm/mkdir).
- Package manager: local paths, file:// Git sources, `latest`/`latest-tag`/
  explicit refs, lockfile reproducibility (install stays locked, update
  advances floating refs), remove/install/update.
- Build-hook configuration contract (project + per-file): parsing, validation,
  malformed-name/value rejection. Actual hook lifecycle execution remains
  unfinished and is NOT claimed complete.
- `niftrc`, history persistence, `nift complete` deterministic engine.
- Performance: full 10k build 0.118s / no-op 0.078s / shared-template 0.137s,
  within or better than the recorded 4.2/4.3-era baselines.
- Valgrind (Linux, 3.26.0-equivalent): process/pipeline/glob/variadic loop and
  the full SQLite package matrix both report 0 errors, 0 bytes in use at exit.

### Package-system dogfood campaign (sqlite rewrite + curl)

Two real packages now exercise the v4.4 package model end-to-end:
- **sqlite** rewritten around an exported `sqlite` struct with callable fields
  (sqlite.available/open/exec/query/transaction); implementation helpers are
  private and isolated. Package source uses script-land syntax (bare fn, //
  comments, bare control flow).
- **curl** created as nift-packages/curl: direct `request()`/`get`/`post`/...
  exports plus a `curl` struct (available/version/features), driving the user's
  `curl` through structured argv (never shell). Verified offline against a
  local HTTP server (GET/POST/PUT/DELETE/HEAD, JSON bodies, custom + repeated
  headers, redirects, Unicode, output download, 404, --no-process denial,
  private-helper isolation) and combined with sqlite in one script.

Language fixes discovered and fixed with regressions: script-land `//` and
`/* */` comments (no @ prefix); JSON-array push accepts quoted strings; bare
script-land call statements returning objects/arrays are fire-and-forget; the
struct-field splitter respects nested lambda/block braces; struct
callable-field dispatch re-quotes quoted args.

Recorded language gaps for package authoring (not implemented this pass): bare
`fn(name(args)) = expr` expression bodies; no object mutation / computed-key
object construction without from_entries/merge; map members cannot have
methods called directly; no JSON-string parser (temp-file workaround); no
try/catch; to_string is numeric-only.

### Final pre-push finalization pass (Nift v4.4.0)

The development version is now **Nift v4.4.0** across every current version
surface (src/CLI.cpp version_text, snap/snapcraft.yaml, guarantee registry,
HANDOVER, README, RELEASES, ReleaseNotes, version-consistency test). The
version-consistency gate passes (executable=4.4.0, Snap=4.4.0). The legacy
regression contract's version assertion was advanced to v4.4.0.

Additional review fixes from this pass:
- Packages may now export a struct or plain-object module value whose callable
  fields are invoked as methods (`vips.resize(...)`) while retaining
  package-private helpers (lambdas snapshot module callables; struct types and
  method module-envs travel with exported instances; callable fields dispatch
  as methods). Fixed an ASan-confirmed struct-method dispatch NULL deref.
- inject() no longer bypasses the NIFT_FS_ROOT/--fs-root restriction (the
  confinement is now a shared file-scope helper across checked_path,
  path_values, ls and inject).
- Windows CreateProcessW backend reviewed; fixed FILE_APPEND_DATA being passed
  as a file attribute (value collides with FILE_ATTRIBUTE_SYSTEM).

Final local certification: make test PASS, independent suite 55/55 PASS,
version gate PASS, website gates + 94-file build PASS, sqlite dogfood PASS,
10k performance ~0.118s full / ~0.077s no-op (no regression), git fsck --full
clean on all five active repos, all working trees clean.

### Campaign-completion status (post-checkpoints 1-11 of the remaining campaign)

Implemented during the completion campaign: interactive TAB/readline-style
editor (Unix) with builtins/PATH/files/identifiers/user-function completion,
history arrows, Ctrl-C/D, PTY tests; native build/project automation bindings
(build/build_all/build_names/build_repair/track/untrack/status/tracked/
project_root) driving ProjectInfo directly with structured results and quiet
automation builds; project + per-file build-hook execution with the agreed
lifecycle, additive mode matching and failure propagation; --no-process across
run/sh/eval/build/hooks plus the --fs-root Nift-native filesystem confinement;
CreateProcessW Windows process backend (code-reviewed, CI-pending);
$[...] command interpolation, `;` separation, explicit `&` failure; package
hardening (entry path traversal, duplicate-add conflict, import isolation);
sqlite query temp-file hardening with mktemp fallback; ASan+UBSan+LSan and
Valgrind passes; website/highlighter/docs completion.

### Genuinely remaining (recorded, not certified here)

- Windows/macOS compile/run verification and the prepared v4.4 cross-platform
  CI workflow (must be pushed and executed); Windows shell-glob is still
  token-passthrough pending a native glob backend.
- Visual browser certification of the highlighter and the green `nift sh`
  prompt (ANSI verified via PTY; rendered visual pending).
- The official `nift-packages` registry repositories are placeholders (no
  manifest at the `sqlite` remote root), so `nift add sqlite` cannot install
  from the registry until they are populated; local/Git-source installs work.
- TSan and macOS/Windows sanitizer runs; the fresh-clone/upstream Git-object
  integrity verification required before release.
- Background jobs/job control (jobs/fg/bg/signals), heredocs, subshells and
  richer command-specific completion remain intentionally out of scope (`&`
  fails explicitly per CP40).
- The v1 sqlite3-CLI backend temp-file uniqueness across concurrent processes
  without mktemp remains a documented limitation; `sqlite3`/`mktemp` are
  runtime dependencies (sqlite3 required; mktemp has a fallback).

## Known unfinished/high-value review work (original ledger, kept for reference)

- ASan+UBSan+LSan (`.build/nift-sanitize`) passed on the v4.4 surfaces (process/pipeline/glob/variadic/shell session/sqlite/automation/hooks and the focused v4.4 source tests) with zero findings; Valgrind passed on process, sqlite, hooks, automation, package-import and shell workloads (0 errors, 0 bytes in use at exit). TSan and macOS/Windows sanitizer runs remain CI-pending;
- complete Windows process/pipeline parity and Windows PATH/glob/completion behaviour;
- CP22 has a deterministic completion engine (`nift complete`) but interactive TAB/readline-style terminal integration still needs implementation/certification;
- CP23 freezes a structured automation boundary; CP24 native `build()/track()/...` script bindings still need the ProjectInfo/CLI extraction rather than subprocess shims;
- CP25/CP26 parse and retain project/file hook declarations, but lifecycle execution, context objects, ordering, failure propagation and incremental semantics still need implementation and regression coverage;
- CP27 currently provides `nift sh --no-process` (and `nift run --no-process` after this review); extend consistently to build and evaluate filesystem-root restriction;
- CP28 highlighter coverage was expanded, but visually certify script comments, `@script`, `@import`, functions/lambdas and shell prompt/cwd styling;
- CP29 has a docs gate; run a broader stale-doc audit and website rebuild;
- CP30 freezes the package manifest/module contract; package-name `@import` resolution is implemented; the registry repositories need real content.

Also run the complete independent regression suite, performance comparison against 4.3 baseline, sanitizers, malformed-input/fuzz-style tests and cross-platform CI before release approval.

## CP31-CP40 completion/review notes

CP31-CP34 now provide the first Git-native package-manager implementation: local packages, arbitrary Git/GitHub sources, the `nift-packages/<name>` short-name registry convention, `add/remove/install/update`, exact-commit locks, `latest`/`latest-tag`/explicit refs, and package-name `@import`. DeepSeek should attack rollback/interruption behavior, dependency conflicts/transitives, Windows symlink/local-package behavior, semantic-version edge cases, offline installs, malicious manifests/path traversal and completion/provenance integration.

CP35-CP37 created the sibling `sqlite` package with `manifest.json`, backend-neutral API and an initial `sqlite3` CLI backend. After this review the real database matrix is green (DDL/DML/query/null/quoting/transactions/errors/large results), SQL parameter substitution uses doubled-quote escaping, query output uses `mktemp` temp files with cleanup, and transactions use `sqlite3 -bail` for atomic rollback. Remaining: cross-process temp uniqueness, `mktemp`/`sqlite3` dependency footprint, Windows.

CP38 adds local-package dogfood. CP39 deliberately leaves native HTTP/fetch out pending real package evidence. CP40 deliberately leaves full job control/heredocs/subshells/command-specific completion out pending dogfood; `&` now fails explicitly rather than silently running synchronously.

Still outstanding from CP21-CP30 remains outstanding unless separately fixed: interactive TAB terminal integration, true native structured build/project bindings, actual project/file hook lifecycle execution, full restriction parity, visual highlighter certification, Windows process/shell/package parity, and the complete sanitizer/ASan/UBSan/LSan/Valgrind + performance + full independent regression campaign.
## Second package-system dogfood campaign (approved review; pre-push STOP)

Certified by the review agent with real tooling. Package policy: every package
is `0.1.0` (early-release, no stability implication). All seven packages use
script-land `//` and `/* */` comments (no template `@` prefix).

### Package repositories (all committed; NOT pushed, NOT tagged)

| package | repo | backend | public API |
|---|---|---|---|
| sqlite | nift-packages/sqlite | sqlite3 CLI | `sqlite.available/open/exec/query/transaction` |
| curl | nift-packages/curl | curl | `request/get/post/put/patch/delete/head` + `curl.available/version/features` |
| postgres | nift-packages/postgres | psql | `postgres.available/open/exec/query/transaction` |
| mysql | nift-packages/mysql | mysql | `mysql.available/open/exec/query/transaction` |
| redis | nift-packages/redis | redis-cli --json | `redis.available/version/open/get/set/del/exists/expire/command` |
| vips | nift-packages/vips | vips + vipsheader | `vips.available/version/resize/thumbnail/convert/crop/rotate/metadata` |
| imagemagick | nift-packages/imagemagick | magick | `magick.available/version/resize/crop/rotate/convert/compose/identify` |

The `imagemagick` package deliberately exports `magick` (package name !=
exported name), regression-covered. Result shapes are `{ok, rows|data, error,
exit_code}` with `127` for missing executables.

### Native script-land Minify++ (new Nift feature, committed)

`minify(...)` is a native builtin delegating to the embedded Minify++ (never a
subprocess; works under `--no-process`; fs-root and project-root enforced;
structured `{ok, output, error, format, written}` failures). Modes:
`minify(source, "js")` string->string for html/css/js/jsx/json/xml/svg;
`minify(path)` -> conventional `.min.ext`; `minify(path, {"output": out})`;
`minify(path, {"in_place": true})`. Mode rule: an explicit format string means
source text; a bare path or options object means a file. CLI parity verified
(script output == `nift minify` output for identical inputs).

### Certification

- `make test` PASS; independent regression suite 61/61 (57 baseline + native
  Minify++, imagemagick->magick, vips+magick combined, database module-style).
- Live dogfood: sqlite, curl, redis (db 15), ImageMagick (resize/crop/rotate/
  convert/identify/compose), combined curl->sqlite->redis->magick pipeline.
- Client-certified (no usable local credentials/tool): postgres, mysql (live
  server unreachable without credentials), vips (executable not installed on
  this machine) - discovery, argv, structured failures, privacy all certified.
- ASan+UBSan+LSan clean on minify + sqlite + combined + tools dogfoods;
  Valgrind clean (0 errors) on minify and multi-package import workloads.
- Performance: 10k full 0.122s / no-op 0.081s / shared-template 0.152s
  (baseline 0.118/0.077/0.148; no regression).
- Version consistency 4.4.0 PASS; all repos `git fsck --full` clean.
- Website: comments/fn-syntax/minify/module-API docs added; highlighter now
  colors bare `//` and `/* */` (template `@//` forms unchanged); 94-file
  baseline preserved; extension + highlight gates PASS.

### Package-design principle (recorded from CP18)

A package earns its existence only if it provides meaningful structured value
over writing the underlying CLI command in Nift shell syntax: structured typed
results, safer argument construction, reusable higher-level behaviour,
backend-independent abstraction, useful state/descriptor handling, or a
substantial reduction in repetitive boilerplate. We deliberately do not
manufacture packages for every pleasant CLI (ffmpeg, pandoc, sass, tsc, ...);
those are already excellent command-style Nift citizens. Databases, HTTP,
Redis and image manipulation are strong initial packages because they supply
structured data and real API value.

### Cross-database API consistency (CP17)

sqlite/postgres/mysql share `available/open/exec/query/transaction` with
`{ok, rows, columns, error, exit_code}` results; differences are documented
(sqlite3 opens a file path; psql/mysql open a connection descriptor; parameter
placeholders differ: `?` for sqlite3, positional `$1, $2` for psql/mysql).
Redis intentionally does not mirror the relational API (client verbs +
`command` escape hatch) because the underlying system is not relational. The
image packages are vips + magick; the imagemagick package exports `magick` to
demonstrate package-name != exported-name.

### Remaining (recorded, not done here)

- Live PostgreSQL/MySQL dogfood needs usable credentials; vips live dogfood
  needs the vips executable - both remain client-certified.
- Package repos and tags still require the approved push (`v0.1.0` later).
- Windows/macOS and visual highlighter certification remain CI-pending.

## Final pre-push hardening pass (CP45-CP55, approved review)

Issues exposed by the seven-package dogfood, addressed before the push stop.

### Transaction atomicity (CP45)
- sqlite `transaction()` live-certified: a failed batch leaves NO partial rows.
- postgres/mysql `transaction()` rewritten to submit ONE atomic
  `BEGIN; s1; s2; ...; COMMIT;` invocation (was per-statement autocommit).
  Deterministically certified via fake-client argv logging; live rollback
  remains client-certified (no authenticated servers).

### Conservative parameter binding (CP46)
- postgres/mysql `$n` and sqlite `?` binders rewritten as SQL-aware scanners:
  placeholders replaced only OUTSIDE string literals, quoted identifiers,
  line/block comments; `$1` can no longer alter `$10` (full digit-run match);
  unmatched placeholders are left verbatim. Textual binding is documented as
  not a prepared statement. Adversarial tests: `$1`/`$10` together,
  literals/comments, apostrophes, backslashes, Unicode, null/bool/number,
  SQL-looking strings. Credentials never logged.

### Language fixes found by package hardening (CP47/CP48 + two bugs)
- `to_string()` generalized to all scalars: string->itself, bool->true/false,
  null->"null"; arrays/objects still error (use stringify()).
- array concatenation with `+`/`+=` (new container, inputs unmutated).
- FIXED: `break`/`continue`/`return` raised inside an `else`/`else-if` branch
  was consumed and lost (infinite loops / dropped breaks in package code).
- FIXED: struct-callable (module-style package method) calls re-wrapped quoted
  string arguments without escaping, corrupting inner quotes/backslashes/
  control chars in SQL-like strings.
- redis package simplified to use array concatenation (manual copy loop gone).

### Certification (CP52)
- `make test` PASS; independent regression suite 63/63 (61 + transaction
  atomicity + parameter binding).
- ASan+UBSan+LSan clean on scalar conversion, array concat, break-in-else,
  struct-callable escape and the tools dogfood; Valgrind clean.
- Performance unchanged: 10k full 0.121s / no-op 0.079s / shared 0.143s.
- Version consistency 4.4.0 PASS. Website rebuilt 94 files; gates PASS.
- curl GitHub description fixed to "HTTP client package".

### Remaining
- Live postgres/mysql rollback and binding verification still require
  authenticated servers; vips still requires the vips executable.
- No pushes, tags or releases performed.

## Executable .f scripts (CP56-CP70, approved review; pre-push STOP)

Completed the Unix scripting story before the push. Executable `.f` scripts
behave like executable shell scripts; the shebang connects `./script.f` back
to Nift exactly as Python/Bash/Ruby do.

### Semantics
- `nift script.f` / `nift ./script.f` / `nift path/to/script.f` are shorthands
  for `nift run script.f` (applied only when the argument resolves to an
  existing file; typoed command names still error). This is what makes
  `#!/usr/bin/env nift` work.
- `nift run script.f` remains explicit Nift source execution (no shebang or
  execute bit required).
- `./script.f` is ordinary OS process execution (shebang + execute bit), never
  in-process Nift evaluation. It works from the host shell, `nift sh`, another
  `.f` script (command-style) and `run()/cmd()`. `./thing.sh`, `./thing.py`
  and binaries follow the same process model.
- Script arguments: user args after the script path are exposed as an immutable
  `args` array (path itself excluded) for both `./deploy.f staging --force`
  and `nift run deploy.f staging --force`.
- A leading `#!` line is stripped by the script runner (script header, not a
  general `#` comment), so the same file runs via `nift run` and `./script.f`.

### Restrictions / POSIX
- `--no-process` blocks external `./script.f` (command-style and `run()`);
  the env restriction also propagates through the shebang chain. `--fs-root`
  does not sandbox child processes (documented).
- POSIX execute-bit semantics: `chmod +x` succeeds, `chmod -x` fails as an
  executable with no fallback to `nift run`. Missing executable, directory and
  bad-shebang use normal external-process behavior. Windows cannot reproduce
  these directly; the shorthand/shebang-strip still work there.
- File-path TAB completion already discovers `./scripts/deploy.f` (including
  paths with spaces); nothing `.f`-specific was needed.

### Certification
- `make test` PASS; independent regression suite 64/64 (63 + executable-script
  contract). Nift source regression `v44_executable_script_smoke.sh` added.
- ASan/UBSan/LSan + Valgrind clean on `nift script.f`, `./script.f`,
  parent->child and `run("./child.f")` paths.
- Performance unchanged (CLI/script dispatch only): full 0.121s / no-op
  0.079s / shared 0.143s.
- Version consistency 4.4.0 PASS. Website rebuilt 94 files; gates PASS.

### Remaining
- No pushes, tags or releases.
