name: Snap

on:
  workflow_call:
  pull_request:
    paths:
      - 'snap/**'
      - 'Makefile'
      - 'src/**'
      - '.github/workflows/snap.yml'
  workflow_dispatch:

env:
  # Exact tested Snapcraft snap revision and version used by the
  # non-publishing toolchain preflight. Live Store promotion is a separate,
  # manual-only workflow (snap-promote.yml) that uses the same pin.
  SNAPCRAFT_SNAP_REVISION: "18514"
  SNAPCRAFT_EXPECTED_VERSION: "9.0.1"

jobs:
  # Non-publishing validation builds for amd64 and arm64. These catch build
  # regressions on pull requests and tag runs but never upload to the Snap
  # Store: the connected Snapcraft/Launchpad build service is the sole producer
  # of published revisions (it builds every platform declared in
  # snap/snapcraft.yaml). No Store credential is present in this job and no
  # channel is read or mutated here, so this job can never wait on remote
  # builders or gate a GitHub release on Snap publication.
  build:
    strategy:
      matrix:
        include:
          - runner: ubuntu-latest
            platform: amd64
          - runner: ubuntu-24.04-arm
            platform: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      - name: Fail-closed version consistency
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "push" ] && [[ "$GITHUB_REF_NAME" == v* ]]; then
            package_version="$(sed -nE "s/^version: '([^']+)'/\1/p" snap/snapcraft.yaml)"
            test "$package_version" = "${GITHUB_REF_NAME#v}"
            python3 scripts/check_version_consistency.py --tag "$GITHUB_REF_NAME"
          else
            python3 scripts/check_version_consistency.py
          fi
      - uses: snapcore/action-build@b391f430cb2650fd359ed4d2cfe88b2c481800e0
        id: snapcraft
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: nift-snap-${{ matrix.platform }}
          path: ${{ steps.snapcraft.outputs.snap }}

  # Non-publishing toolchain preflight: installs the pinned Snapcraft snap
  # revision and asserts both its version and revision, using no Store
  # credentials and performing no channel release or Store mutation.
  # A manual workflow_dispatch therefore exercises the same pinned toolchain
  # used by the separate manual Store-promotion workflow.
  toolchain-preflight:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      - name: Install and verify pinned snapcraft
        shell: bash
        run: bash packaging/snapcraft-pin.sh
