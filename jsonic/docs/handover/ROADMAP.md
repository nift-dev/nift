# Jsonic++ roadmap

Near-term evidence priorities are established JSON conformance corpora, coverage-guided fuzzing, repeated sanitizer/leak campaigns, differential parser comparisons, and API/package usability checks. Feature growth is intentionally secondary to proving the existing JSON contract.
